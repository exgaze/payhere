from django.urls import path
from .views import URLShortnerView, RedirectView

urlpatterns = [
        #path('', URLShortnerView.as_view()),
        #path('/chilpit/<str:url>', RedirectView.as_view())
        ]
